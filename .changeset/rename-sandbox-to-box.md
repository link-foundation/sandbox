---
bump: major
---

Rename everything from sandbox to box: Docker image names (konard/sandbox-* → konard/box-*), Unix user (sandbox → box), home directory (/workspace → /home/box), and directory names (essentials-sandbox → essentials-box, full-sandbox → full-box). All markdown documentation updated to reflect the new naming.
